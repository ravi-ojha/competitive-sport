#include<stdio.h>
int main()
{
    int N,Q;
    scanf(" %d %d");
    int row[N+1], col[N+1];
    while(Q--)
    {
        char query[10];
        scanf(" %s",query);
        printf(" %s",query);
        if(query[0] == 'R')
        {
            if(query[3] == 'S')
            {
                scanf("%d %d",&p,&q);
                if(q==0)
            }
        }
    }
}
